# Rust build output
/target

# Model artifacts: regenerate with run.cmd setup
/models
/models_fp32

# Vendored Tesseract runtime: regenerate with run.cmd setup
/vendor

# Packaged output
/dist

# Build-time-only Python venv for the model export
/.venv-export

# Never commit weights, whatever the layout
*.onnx
*.onnx_data
*.traineddata
*.safetensors

# Editor / tooling local state
/.serena/project.local.yml
.vscode/
.idea/
